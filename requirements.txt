# Landed — pinned dependencies
# Full lockfile before submission:  pip freeze > requirements.lock.txt

# --- Domain core ---
pydantic==2.9.2

# --- Document ingestion ---
pdfplumber==0.11.4          # PDF text with page anchors
pypdfium2==4.30.0           # render scanned pages to images (self-contained wheel)
python-docx==1.1.2          # DOCX paragraphs and tables
openpyxl==3.1.5             # XLSX with sheet/cell refs
Pillow==10.4.0
pandas==2.2.3

# --- Extraction (the only layer that calls a model) ---
anthropic==0.39.0

# --- Web ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
jinja2==3.1.4
python-multipart==0.0.12    # file uploads
itsdangerous==2.2.0         # signed session cookies

# --- Persistence ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3      # PostgreSQL driver (psycopg 3)
alembic==1.14.0             # schema migrations

# --- Auth ---
argon2-cffi==23.1.0

# --- Reporting ---
reportlab==4.2.5            # PDF export (pure Python, no system deps)

# --- Config ---
python-dotenv==1.0.1
pydantic-settings==2.6.1

# --- Dev ---
pytest==8.3.3
httpx==0.27.2               # FastAPI TestClient
ruff==0.7.2
